import MathQL

/-- Placeholder entry point: the front end (parser + type-checker) is in place;
    the SQL back end is not yet wired up. -/
def main : IO Unit :=
  IO.println "mathql: front end only — SQL back end not yet implemented"
